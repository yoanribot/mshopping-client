import React, { memo, useState, useEffect, useMemo } from 'react';

const Wishes = memo(({}) => {
  const [state, setstate] = useState();

  useEffect(() => {}, []);

  return <>Functional Wishes component structure mock up</>;
});

export default Wishes;
